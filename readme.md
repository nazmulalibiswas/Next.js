# Next.js with @nazmulalibiswas - The react framework for the Web!
## Topics
- List of Topics
  - Introduction (Overview of the Next.js and what you will build!)
  - Hello World (Creating your first basic page to test the environment)
  - Project Structure (Understanding the files and folders generated by Next.js)
  - Before We Start (Essential for prerequisites and basic setup knowledge)
  - Routing (How Next.js turns your folders into website URLs)
  - Nested Routes (Creating folders inside folders for deeper URL paths)
  - Dynamic Routes (Using placeholders in URLs for data that changes)
  - Nested Dynamic Routes (Combining nested paths with placeholders)
  - Catch All Segments (Capturing multiple URL segments at once)
  - Not Found Page (Creating a custom 404 error page for missing URLs)
  - File Colocation (Safely keeping UI components in the same folder as your routes)
  - Private Folders (Hiding specific folders from the router using an underscore)
  - Route Groups (Organizing folders logically without changing the actual URL path using parentheses)
  - Layouts (Creating shared UI elements `like headers and footers` that persist across pages)
  - Nested Layouts (Adding specific layouts to a subsection of your app)
  - Multiple Root Layouts (Creating completely different base layouts for different parts of your site)
  - Routing Metadata (Adding SEO details `like page titles and descriptions` to your routes)
  - Title Metadata (Dynamically generating browser tab titles)
  - Link Component (Using `<Link>` instead of `<a>` tags for fast, client side page transitions)
  - Active Links (Highlighting the navigation menu item for the page the user is currently on)
  - Params and SearchParams (Reading data from the URL like `IDs` or `?query=string`)
  - Navigating Programmatically (Triggering page changes using code)
  - Templates (Similar to layouts, but they force the UI to completely rebuild on every page change)
  - Loading UI (Showing an automatic fallback screen `like a spinner` while a page loads)
  - Error Handling (Catching unexpected bugs using an `error.tsx` file)
  - Recovering from Errors (Adding a `"Try Again"` button to reload the failed component)
  - Handling Errors in Nested Routes (Isolating crashes so a broken section doesn't crash the whole page)
  - Handling Errors in Layouts (Managing errors that happen inside your shared layout files)
  - Handling Global Errors (Catching critical app wide crashes with a global `error.tsx` file)
  - Parallel Routes (Showing multiple independent pages/views in the same layout simultaneously)
  - Unmatched Routes (Managing 404s specifically within complex parallel routing setups)
  - Conditional Routes (Displaying different views based on specific conditions `like user login status`)
  - Intercepting Routes (Loading a route in a modal `like a popup image` while keeping the background intact)
  - Parallel Intercepting Routes (Combining modals and parallel views for advanced UI patterns)
  - Route Handlers (Creating backend API endpoints directly inside Next.js)
  - Handling GET Requests (Building APIs to send data back to the client)
  - Handling POST Requests (Building APIs to receive and save new data)
  - Dynamic Route Handlers (Creating APIs that respond to changing URL parameters)
  - Handling PATCH Requests (Building APIs to update existing data)
  - Handling DELETE Requests (Building APIs to remove data)
  - URL Query Parameters (Reading search parameters `?limit=10` inside your API routes)
  - Headers in Route Handlers (Reading and setting HTTP headers for security or metadata)
  - Cookies in Route Handlers (Reading and storing cookies for things like session tracking)
  - Redirects in Route Handlers (Sending a user to a different URL automatically from an API)
  - Caching in Route Handlers (Saving API responses so subsequent requests are faster)
  - Middleware (Running code `like auth checks` before a request even finishes completing)
  - Rendering (Understanding how Next.js turns React code into viewable HTML)
  - Client-side Rendering (CSR) (Rendering the website entirely in the user's browser using JavaScript)
  - Server-side Rendering (SSR) (Generating the HTML on the server for faster load times and better SEO)
  - Suspense SSR (Sending a loading state from the server while the rest of the page finishes building)
  - React Server Components (Next.js 15's default components that run entirely on the server with zero client JS)
  - Server and Client Components (Understanding the difference between server only UI and interactive browser UI)
  - RSC Rendering Lifecycle (The step by step process of how Server Components are built and sent to the browser)
  - Static Rendering (Pre building pages exactly once during deployment for maximum speed)
  - Dynamic Rendering (Building a page freshly every single time a user requests it)
  - generateStaticParams (Pre building specific dynamic routes ahead of time for performance)
  - dynamicParams (Controlling what happens when a user visits a dynamic route that wasn't pre built)
  - Streaming (Breaking a page into chunks and sending them to the browser as soon as they are ready)
  - Server and Client Composition Patterns (Best practices for combining server and client logic in the same app)
  - Server only Code (Enforcing strict security by preventing database code from leaking to the browser)
  - Third Party Packages (How to safely use NPM packages that might not support Server Components natively)
  - Context Providers (Setting up global React State `Context API` in a Server Component environment)
  - Client only Code (Ensuring interactive code runs only in the browser to avoid server errors)
  - Client Component Placement (Pushing `"use client"` down the component tree to maximize server performance)
  - Interleaving Server and Client Components (How to correctly pass a Server Component as a child to a Client Component)
  - Data Fetching (The basics of requesting data from an external source or database)
  - Fetching Data in Client Components (Grabbing data directly from the browser `the traditional React way`)
  - Fetching Data in Server Components (Grabbing data directly from the server `the Next.js App Router way`)
  - Loading and Error States (Handling UI while waiting for data or dealing with failed fetches)
  - Sequential Data Fetching (Fetching data one after the other `waterfall`)
  - Parallel Data Fetching (Fetching multiple data sources at the exact same time for better speed)
  - Fetching from a Database (Connecting your Next.js app to a real database `like PostgreSQL or MongoDB`)
  - Data Mutations (The concept of changing or updating data in your application)
  - Forms with Server Actions (Handling form submissions securely on the server without needing API routes)
  - Pending State with useFormStatus (Showing a `Submitting...` button while a Server Action runs)
  - Form Validation with useActionState (Checking for errors `like empty fields` and returning validation messages)
  - Separating Server Actions (Moving your server functions into isolated files for cleaner code)
  - useFormStatus vs useActionState (Understanding when to use which hook for form management)
  - Update Server Action (Using an action specifically to modify existing database records)
  - Delete Server Action (Using an action specifically to delete records)
  - Optimistic Updates (Updating the UI instantly before the server confirms the change to make the app feel faster)
  - Form Component (Building reusable form elements for scalable applications)
  - Authentication (The theory behind logging users in and keeping data secure)
  - Clerk Setup (Installing and configuring Clerk `a popular authentication provider` in Next.js)
  - Sign In and Sign out (Creating the logic for users to log in and end their sessions)
  - Profile Settings (Allowing users to view and update their account information)
  - Conditional UI Rendering (Showing different buttons or layouts depending on if the user is logged in)
  - Protecting Routes (Forcing users to log in before they can visit specific pages `like a dashboard`)
  - Read Session and User Data (Extracting the current user's ID or email from Clerk to use in your app)
  - Role Based Access Control (Restricting pages so only specific users like `"Admins"` can access them)
  - Customizing Clerk Components (Styling the default Clerk login forms to match your brand)
  - Deploying Next.js Apps to Vercel (Pushing your finished code live to the internet for the world to see)

# Contributing
Contributions are always welcome! Feel free to fork this repository, create a new branch, and submit a pull request.

# Author
- Md. Nazmul Ali Biswas
- Fullstack Engineer & Tech Enthusiast
- Web: `www.nazmulalibiswas.com`
- Contact Info: `nazmulalibiswas.dev@gmail.com | hireme@nazmulalibiswas.com`

